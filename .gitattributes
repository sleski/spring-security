# See https://help.github.com/articles/dealing-with-line-endings for more information

# Set default behaviour, in case users don't have core.autocrlf set.
* text=auto

# Java files are text files, line ending should be lf
*.java text eol=lf

# xml, xsd files are text files, line ending should always be lf
*.xml text eol=lf
*.xsd text eol=lf

# jsp files are text files, line ending should always be lf
*.jsp text eol=lf

# properties files are text files, line ending should always be lf
*.properties text eol=lf

# graphviz files ending with .gv are text files ending with lf
*.gv text eol=lf

# Denote all files that are truly binary and should not be modified.
*.png binary
*.jpg binary

